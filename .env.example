# 지점 정보
STORE_ID=oryudong

# NVR/DVR RTSP 접속 정보
RTSP_HOST=192.168.0.100
RTSP_PORT=554
RTSP_USERNAME=admin
# 주의: 비밀번호에 특수문자(@, :, /, #, ?)가 있으면 URL 인코딩 필요!
# 예: P@ssw0rd! → P%40ssw0rd%21
# 인코딩 참고: @ → %40, : → %3A, / → %2F, # → %23, ? → %3F, ! → %21
RTSP_PASSWORD=your_nvr_password

# 활성 채널 (쉼표 구분)
ACTIVE_CHANNELS=1,2,3,4

# Supabase
SUPABASE_URL=https://xxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Cloudflare Tunnel (선택 - 도메인 있을 때만)
# docker compose --profile cloudflare up -d 로 실행
# CLOUDFLARE_TUNNEL_TOKEN=eyJ...

# go2rtc API 인증 (선택)
API_USERNAME=admin
API_PASSWORD=secure_password

# TURN 서버 (WebRTC NAT 통과용, 선택)
TURN_URL=turn:a.turn.metered.ca:443?transport=tcp
TURN_USERNAME=
TURN_CREDENTIAL=
